apiVersion: apps/v1
kind: Deployment
metadata:
  name: tus-server
  namespace: backend
spec:
  selector:
    matchLabels:
      app: tus-server
  replicas: 1
  template:
    metadata:
      labels:
        app: tus-server
    spec:
      imagePullSecrets:
      - name: dockercreds
      containers:
      - name: tusserver
        image: localhost:5000/tus_server:63
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 1080
        envFrom:
        - secretRef:
            name: backend-secret
        - secretRef:
            name: tus-secret

